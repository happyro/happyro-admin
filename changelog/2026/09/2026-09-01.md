# 2026-09-01

## Added

- `[admin]` 初始化 Laravel 13 与 Ant Design Pro 6.0.3 管理后台。
- `[admin]` 增加本机物理部署使用的 systemd 服务。
- `[backend]` 增加独立的 `happyro_admin` MariaDB 数据库。
- `[backend]` 增加 Laravel Session 与 Sanctum 账号密码登录。
- `[backend]` 增加可依赖注入的认证、登录限流、权限和审计服务。
- `[backend]` 增加无 GM 前缀的用户、角色、权限和审计表。
- `[backend]` 增加 `gm:user:create` 管理账号创建命令。
- `[backend]` 增加可替换的玩家账号仓储、查询、资料更新、密码重置和状态管理 API。（未提交）
- `[frontend]` 增加“用户管理”侧栏及用户账号、游戏角色、登录日志三个子菜单。（未提交）
- `[backend]` 增加游戏角色和登录日志查询 API，并按资源拆分控制器。（未提交）
- `[backend]` 增加玩家管理 Service 的 Mock 单元测试。（未提交）
- `[backend]` 增加玩家账号注册接口和注册审计事件。（未提交）

## Changed

- `[frontend]` 登录页沿用 Ant Design Pro 响应式布局，仅保留账号密码登录。
- `[frontend]` 应用标题改为 HappyRO，登录页使用本地 RO 图片并移除模板元数据页脚。
- `[frontend]` 精简未接入后端的示例路由，默认登录后进入欢迎页。
- `[admin]` 将玩家账号访问独立到 `game` 数据库连接，不与后台管理员库混用。（未提交）
- `[backend]` 将玩家登录日志连接到独立的 `happyro_log` 数据库。（未提交）
- `[frontend]` 将用户管理侧栏菜单和账号详情字段接入国际化资源。（未提交）
- `[frontend]` 修正用户管理侧栏菜单键，避免显示原始国际化 key。（未提交）
- `[backend]` 对齐用户账号、游戏角色和登录日志查询参数，修复筛选无效问题。（未提交）
- `[backend]` 通过可注入 Hasher 完成玩家密码处理，并补充注册、重置密码和更新用例测试。（未提交）
- `[frontend]` 将用户管理新增可见文案接入中英文国际化资源。（未提交）
- `[frontend]` 统一用户账号、游戏角色和登录日志的账号检索字段为“用户名”。（未提交）
- `[frontend]` 为游戏角色补充职业、在线状态和地图名称的本地化显示。（未提交）

## Fixed

- `[frontend]` 修复登录页在窄屏设备上的水平溢出。
- `[frontend]` 修复登录后示例接口失败导致错误提示反复弹出。
- `[frontend]` 使用标准 ICO 和版本化链接修复浏览器标签图标缓存。

## Documentation

- `[admin]` 增加 HappyRO Admin 仓库级代理规则和按日期维护的 changelog 目录。
- `[admin]` 增加文件、函数、依赖注入、可 Mock、分层设计和多入口复用的代码实现规范。
- `[admin]` 在 HappyRO 根仓库代理规则中引用管理后台的独立实现规范和 changelog 目录。
- `[admin]` 增加 `docs/product/` 产品文档目录、`progress.md` 项目总进度和玩家查询设计占位文档。
- `[admin]` 增加用户管理子菜单、操作按钮和 SQL 数据源待办说明。（未提交）
- `[admin]` 规定业务映射以 HappyRO 项目资源为准，并在管理后台保留带来源说明的冗余副本。（未提交）
- `[frontend]` 将职业和地图映射迁移到 `frontend/src/data/game/`，并本地化登录日志成功提示。（未提交）
- `[frontend]` 兼容登录日志成功文本的空白和大小写差异，确保中文显示“登录成功”。（未提交）
- `[backend]` 增加玩家账号删除、批量封禁/解禁和踢下线接口，并写入操作审计。（未提交）
- `[frontend]` 增加玩家账号单条删除二次确认及批量操作按钮，所有文案支持中英文。（未提交）
- `[frontend]` 将注册账号置于工具栏左侧，并将批量操作收纳到下拉菜单中。（未提交）
- `[admin]` 修复 Umi 开发服务运行缓存目录权限，避免 OpenAPI 插件启动时反复出现 `EACCES` 错误页。（未提交）
- `[frontend]` 区分账号封禁与解禁操作图标，避免两种状态显示相同图标。（未提交）
- `[frontend]` 修正操作下拉菜单中的解禁图标，改用启用状态图标。（未提交）
- `[backend]` 修复玩家账号删除遗留角色、更新不存在账号返回 500 以及批量删除数量不准确的问题。
- `[frontend]` 清理玩家账号批量操作已废弃的国际化键。
- `[admin]` 移除 Laravel 默认示例测试，避免模板测试混入产品测试结果。
- `[frontend]` 将示例页面反馈组件改为使用 Ant Design App 上下文，并同步更新测试 Mock。
