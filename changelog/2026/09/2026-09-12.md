# 2026-09-12

## Changed

- `[frontend]` 运营管理将物品发放和 Zeny 发放拆成两个菜单。

## Fixed

- `[backend]` 冒险工具物品详情与列表使用同一 client 物品范围；发放仍按 server 范围查找。
- `[backend]` 冒险工具权限缺配置时默认管理员策略，不再默认所有人可用。
- `[backend]` 游戏设置修订 `reason` 改为 `remark` 的迁移在 MariaDB 与 PHPUnit SQLite 测试库上均可执行。
- `[frontend]` `UMI_ENV=test` 的 API 代理改为本机 `127.0.0.1:18081`。
