# 2026-09-13

## Changed

- `[frontend]` 删除可变游戏资料版本配置及历史页签，版本固定由配置提供；右上角保留亮色/暗色主题切换按钮并移除文档、多语言、个人设置及悬浮设置入口。

- `[admin]` 移除系统设置中的游戏资料配置页和版本修改接口，资料版本固定使用 happyro.php 配置；移除遗留写入逻辑，保留历史审计查询。
- `[frontend]` 菜单右上角新增直接切换亮色/暗色主题的按钮，不恢复主题设置抽屉。

- `[frontend]` 魔物与物品图鉴列表图片支持点击打开详情，与地图列表交互一致。

- `[frontend]` 地图缩略图点击打开详情，游戏支持状态改为列表列项；地图范围移入顶部下拉搜索条件，常用地图按冒险工具顺序优先展示，顺序来源为 happyro-client MapCatalogData.js。

- `[frontend]` 顶部使用 favicon 并显示实际账号；移除文档、版本、语言、个人设置、OpenAPI 和主题设置入口，固定简体中文。

- `[admin]` 地图查询与游戏共用地图目录和名称，默认按游戏分线配置显示可用地图，保留全部服务端地图筛选；数字编号移入详情，增加无图片地图的地形预览与列表错误提示。数据由根仓库 tools/generate-map-catalog.mjs 生成。

- `[frontend]` 角色操作统一称为“角色属性”，分开职业、等级、技能点与基础属性修改；职业使用与客户端一致的可搜索名称列表，补齐进阶及扩展职业映射，提交只发送变化字段并防止重复提交。
- `[frontend]` 角色详情使用中文字段与职业、地图名称；物品发放与目标角色选择优先展示名称，召唤目标改为实际在线角色选择，并隔离过期搜索响应。
- `[frontend]` 每次打开角色属性重新读取数据，修正转职及技能点命令返回字段与查询字段的映射；6 项表单检查及 TypeScript 检查通过。

- `[frontend]` 移除概览中的通用宣传文案，保留版本标签和实际管理入口。

- `[frontend]` 将首页“欢迎回来”卡片改为“概览”，并将客户端资源与服务端版本以内联标签放入概览。

- `[frontend]` 首页增加当前客户端资源 `kro-20211105` 和服务端版本 `2fe6ab3dc4` 信息。

- `[frontend]` 页面主标题由“HappyRO 管理后台”改为“首页”，与菜单名称一致。

- `[frontend]` 首页主标题使用 24px 半粗体，“欢迎回来”与“常用入口”统一使用卡片标题样式。

- `[frontend]` 菜单使用“首页”，页面标题改为“HappyRO 管理后台”，欢迎语不再附带用户名，常用入口卡片统一行高。

- `[frontend]` 将欢迎页置于菜单首项，替换模板教程为 HappyRO 日常管理介绍与按权限展示的常用入口，移除旧 Markdown 专用样式。
