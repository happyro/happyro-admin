# 2026-09-22

## Changed

- `[backend]` 特性维护校验范围调整为 0—32767；`[frontend]` 同步说明直接设置不受点数预算限制。
- `[frontend]` 注册账号与重置密码统一左对齐标签、等宽描边输入框、字段间距及标题下方 24px 留白，窄屏自动上下排列。
- `[frontend]` 注册性别使用“男 / 女”选项并提交 M / F；映射依据账号接口与服务端 login.sex 字段，副本位于 src/data/game/sex.ts。
- `[admin]` 离线角色可打开角色属性并读取保存数据，仅在实际修改时检查在线状态；提交前刷新状态，支持角色重新上线后继续操作。
- `[frontend]` 角色属性采用宽版六标签页，默认打开职业页；当前职业和目标职业卡片上下排列，当前职业名称使用 18px 正常字重，统一技能、状态恢复及表单布局。
- `[frontend]` 点数操作支持素质点和技能点清零、实时上限校验和仅提交修改字段；降低等级、重置及转职操作需确认，成功后刷新状态并保留弹窗。
- `[frontend]` 角色详情采用正常字重的分组表格，显示地图名称和代码、四转六项特性、剩余特性点及 AP；后端补齐对应字段。
- `[frontend]` 位置操作仅保留传送，地图及 NPC 提示统一使用“目标位置：”和中文地图名称，上下各留白 24px；隐藏 NPC 和不支持的地图不显示操作入口。
- `[frontend]` 地图详情 NPC 列表采用固定图片列、弹性信息列及右对齐操作列；长名称换行，窄屏将操作放到坐标下方。

## Added

- `[frontend]` 转职选项按冒险工具的一转、二转、三转、四转、初心者、扩展职业及其它分组，仅显示职业名称并支持搜索；分类副本来自客户端 JobConst.js 与 JobPropertyTable.js，在 src/data/game/job-group-catalog.json 记录来源和哈希，优先使用实时特性标记识别四转。
- `[frontend]` 魔物图鉴展示出现地图、常驻数量和传送入口，地图详情的可见 NPC 同步提供传送入口；刷新点副本来自客户端 applications/pwa/data/monsters/catalog.json，按 data/navigation/map-catalog.json 可用地图筛选，在 src/data/game/monster-spawns.json 记录来源及 SHA-256。
- `[frontend]` 根据客户端 src/DB/Map/MapNameTranslations.js 的 Airship → 飞空艇补齐 airplane 中文映射，传送提示优先使用本地中文映射。
