# HappyRO Admin 代理说明

本仓库是 HappyRO Web 栈的内部游戏管理后台，采用单仓库双应用结构：

- `backend/`：Laravel API
- `frontend/`：Ant Design Pro 前端
- `deploy/`：本机物理部署配置

子目录中的 `AGENTS.md` 在对应范围内继续生效；本文件规定整个仓库共同遵守的规则。

## Git 提交规范

- HappyRO 自有提交必须使用 `type(scope): subject` 格式。
- `scope` 必须存在并使用小写英文。
- 破坏性变更使用 `type(scope)!: subject`，并在提交正文中说明迁移方式。
- 允许的 `type`：`feat`、`fix`、`config`、`docs`、`refactor`、`test`、`build`、`ci`、`chore`、`perf`、`style`、`revert`。
- `subject` 使用祈使语气的英文，不以句号结尾。
- 提交首行总长度不得超过 72 个字符。
- 一个提交只包含一个逻辑变更；上游合并提交和上游作者提交不受此限制。
- 产品改动如果需要提交，必须自动添加或更新本仓库 `changelog/` 目录中的记录；只有用户明确说明不写 changelog 时才可跳过。
- changelog 必须与对应的实际变更包含在同一个提交中，禁止创建只包含 changelog 的独立提交。
- `main` 是长期中文产品分支，也是默认维护分支。
- 未经用户明确要求，不得提交或推送。

## 代码实现规范

- 默认按当前最佳实践实现，遵循 Laravel 的设计原则和项目既有架构。
- 单个源代码文件原则上不得超过 800 行；单个函数或方法原则上不得超过 150 行。超过上限时，必须在代码评审或提交说明中解释原因，并优先拆分职责。
- 代码必须通过依赖注入组织协作关系，业务依赖不得在类内部硬编码实例化。
- 业务组件、Service、Repository、外部适配器和基础设施依赖必须可替换、可 Mock，并能编写独立单元测试。
- 代码必须有清晰的分层和职责边界：控制器只负责协议适配、输入校验、授权入口和响应转换，不得编写业务逻辑。
- 可复用业务逻辑必须放在 Service 或领域组件中；数据访问放在 Repository 或明确的数据访问层中。
- 一个功能可能同时被 CLI、HTTP API 或其他入口调用时，核心用例必须独立于入口层，CLI 和控制器只负责组装输入并调用同一 Service。
- 新增功能应同时考虑单元测试和必要的集成测试；测试不得依赖不可替换的全局状态或真实外部服务。
- 不为了历史写法、旧接口、旧字段、旧环境变量、旧参数名或临时方案保留兼容分支。
- 不添加未被当前需求使用的兜底逻辑、迁移逻辑、别名逻辑或废弃路径。
- 只有用户明确要求兼容已有生产用法时，才保留或新增兼容实现，并在文档中写清楚兼容范围。
- 命令行工具不带任何参数运行时，必须只输出使用说明和常用例子，不执行实际业务动作。
- 命令行工具的帮助输出默认启用友好的 ANSI 终端高亮：标题使用亮青色加粗 `1;36`，分节标题使用亮黄色加粗 `1;33`，子命令使用亮绿色加粗 `1;32`，常用例子使用青色 `36`，并使用 `0` 重置样式。
- 命令行工具必须提供禁用颜色的选项，例如 `--no-color`，用于日志、管道或不支持 ANSI 的终端。
- 命令行工具的帮助输出首行和尾行必须为空行。

## 业务映射规范

- 职业、地图、物品、状态等业务映射必须以 HappyRO 项目已有的代码或资源为准，禁止凭经验新增可能不一致的名称或 ID。
- 外部 HappyRO 项目的映射在 `happyro-admin` 中必须保留一份冗余副本；副本需要记录来源文件，更新时同步核对来源并在 changelog 中说明变更。
- 映射数据必须放在独立的数据目录中，与页面、控制器和业务逻辑分离；当前前端映射统一放在 `frontend/src/data/game/`，多语言名称放在国际化资源中，映射表只保存稳定的 ID、代码和翻译键。

## Changelog

- 项目变更必须在本仓库的 `changelog/` 目录记录，并与实际变更保持一致。
- 日志路径固定为 `changelog/<年>/<月>/YYYY-MM-DD.md`，同一天的修改追加到同一个文件。
- 日期日志按变更性质使用 `Added`、`Changed`、`Fixed`、`Documentation` 等英文二级标题分类，不得使用无分类的平铺列表。
- 每条记录按归属以 `` `[admin]` ``、`` `[backend]` `` 或 `` `[frontend]` `` 标记：跨应用和仓库级变更使用 `` `[admin]` ``，Laravel 后端使用 `` `[backend]` ``，Ant Design Pro 前端使用 `` `[frontend]` ``。
